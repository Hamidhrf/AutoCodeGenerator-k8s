## UI Code (React)

* This code reads prompts from question.json file under public folder and sends it to inference layer (LLM) and displays answer.
* LLM takes around 2 minutes to process each request. 
* Once all requests are processed, it can be checked by clicking on Result button after approx 200 min.